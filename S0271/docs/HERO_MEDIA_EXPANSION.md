# Step 06 ? Superseded

The approved Header/Hero redesign removes scroll-driven media expansion. The expansion module, initialization and crop styles have been removed. The Hero image is now large and static at every viewport size and under reduced motion. See HERO_EDITORIAL.md for current behavior and validation limits.
