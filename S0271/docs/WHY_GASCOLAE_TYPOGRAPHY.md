# Step 13 — Typographic architecture

Four large words (DETECT, LOCATE, VERIFY, TRACE) form an alternating typographic column. Alongside it, the three existing `whyGascolae` entries explain the measurement chain, integrated traceable data and transparent service boundaries. Mobile places the word composition above the content. The decorative English words are hidden from assistive technology; the Vietnamese headings and explanations remain semantic text.

The measurement-chain qualification stays visible. Scope wording explicitly says no replacement of Method 21, no verification certificate and no issuance of carbon credits. There are no videos, images, client logos, awards, certifications or new performance claims. No runtime JavaScript or dependency is needed.

Generate with `node S0271/scripts/build-why-gascolae.mjs`; append `--check` to validate synchronization. Passed: generator/syntax checks, three principles, four words, balanced HTML, unique IDs and no media in this section. Real browser visual/responsive QA remains pending because no browser connection is available in this session.
